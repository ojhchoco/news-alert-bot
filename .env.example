# ===========================================
# News Alert Bot - 환경변수 예시
# 이 파일을 복사해 .env 로 저장한 뒤 값을 채워주세요.
# ===========================================

# ---- 네이버 검색 API (뉴스 검색 시 필수) ----
NAVER_CLIENT_ID=your_naver_client_id
NAVER_CLIENT_SECRET=your_naver_client_secret

# ---- Slack (선택) ----
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/URL

# ---- Google News RSS (글로벌 뉴스용, API 키 불필요) ----
# 언어/지역: ko=한국, en=영어(글로벌), ja=일본어, vi=베트남어
GOOGLE_NEWS_HL=ko
GOOGLE_NEWS_GL=KR
GOOGLE_NEWS_CEID=KR:ko

# ---- Google Custom Search (연구/정부/국제기구 자료 검색용) ----
# Programmable Search Engine 에서 검색엔진 생성 후 ID와 API 키 발급
GOOGLE_CSE_ID=your_custom_search_engine_id
GOOGLE_API_KEY=your_google_api_key

# ---- 접근 제한 (선택, 둘 다 없으면 인증 없이 모두 접근 가능) ----
# 비밀 키 방식: APP_SECRET_KEY 만 설정. 접속 시 URL에 ?key=비밀키 붙이기.
# APP_SECRET_KEY=your_long_secret_string
# Basic 인증 방식: 아이디/비밀 입력 시 접속 가능 (브라우저 로그인 창).
# APP_BASIC_USER=admin
# APP_BASIC_PASSWORD=your_password

# ===========================================
# CSE(커스텀 검색엔진)에 넣으면 좋은 도메인 예시
# 검색엔진 설정 > "검색할 사이트"에 아래처럼 추가하면
# 해당 기관/정부/국제기구 자료 위주로 검색됩니다.
# ===========================================
#
# 국제기구·UN
#   *.un.org
#   *.who.int
#   *.ilo.org
#   *.unesco.org
#   *.wto.org
#   *.imf.org
#   *.worldbank.org
#   *.oecd.org
#
# 연구소·학술
#   *.nature.com
#   *.sciencedirect.com
#   *.reuters.com
#   *.brookings.edu
#   *.rand.org
#
# 정부·공공 (한국)
#   *.go.kr
#   *.korea.kr
#   *.assembly.go.kr
#
# 정부·공공 (해외 예시)
#   *.gov
#   *.gov.uk
#   *.europa.eu
#
# ===========================================
